# Shop Backend

Проект представляет собой серверную часть интернет-магазина, разработанную на Python с использованием современных инструментов автоматизации и проверки качества кода.

## 🛠️ Стек технологий
* **Язык**: Python 3.10+
* **Менеджер пакетов**: Poetry
* **Форматирование и стиль**: Black, isort, Flake8
* **Статический анализ типов**: Mypy
* **Тестирование**: Pytest, pytest-cov

## 📦 Структура проекта
* `src/` — исходный код приложения (модели `Product`, `Category` и утилита `utils.py`).
* `tests/` — раздельные модульные тесты для проверки бизнес-логики и утилит.
* `main.py` — проверочный скрипт для верификации работы моделей и чтения JSON.
* `products.json` — файл с исходными данными для подгрузки товаров.

## 🚀 Установка и запуск

1. Клонируйте репозиторий и перейдите в папку проекта:
```bash
git clone <ссылка_на_ваш_репозиторий>
cd shop_backend
```

2. Установите зависимости разработки через Poetry:
```bash
poetry install
```

3. Запустите комплексную автоматическую проверку кода (линтеры, форматирование, статический анализ и тесты с генерацией отчёта):
```bash
poetry run poe fix
```

4. Для проверки базовой работоспособности и чтения JSON запустите скрипт:
```bash
poetry run python main.py
```

## 📊 Тестирование и покрытие
Проект полностью покрыт тестами (>75%). Для генерации и просмотра подробного HTML-отчета о покрытии запустите команду автоматизации. После её выполнения откройте файл `htmlcov/index.html` в вашем браузере.

